The default virtual environment is located at /opt/hermes/.venv.
§
The default kanban tasks are assigned to the default profile.
§
The python-docx library is available only via the terminal tool; it is not available in the execute_code sandbox. The default directory for saving .docx files is /opt/ai_files/.
§
The memory tool requires the 'action' parameter ('add', 'replace', or 'remove') to be explicitly specified for single-operation updates to ensure correct parsing.