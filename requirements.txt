#these requirements are for the literature review, this connects to lightrag_ingest.py
psycopg2-binary==2.9.13

sudo apt install python3-venv
cd ~/local_hermes
python3 -m venv .venv
source .venv/bin/activate
which python
#should say /home/mep635/local_hermes/.venv/bin/python
python -m pip install psycopg2-binary
python -c "import psycopg2; print('psychopg2 OK')"
# should return: "psycopg2 OK"
